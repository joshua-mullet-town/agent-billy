# Agent Billy Configuration

# GitHub Personal Access Token (required for GitHub operations)
# Create at: https://github.com/settings/tokens
# Required scopes: repo, issues, pull_requests
GITHUB_TOKEN=your_github_token_here

# Default repository settings (optional)
DEFAULT_GITHUB_OWNER=your-username
DEFAULT_GITHUB_REPO=your-repo-name

# Agent configuration (optional)
AGENT_USERNAME=agent-billy
AGENT_MAX_CONCURRENT_TASKS=3

# LLM Configuration (optional)
LLM_MODEL=claude
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=500

# OpenAI API Key (if using GPT models)
OPENAI_API_KEY=your_openai_key_here

# Anthropic API Key (if using Claude models)
ANTHROPIC_API_KEY=your_anthropic_key_here